<?xml version="1.0" encoding="UTF-8"?>
<templates id="template">
<t t-name="rbac.AccessStudio">
<div class="as_wrap">

    <!-- ── Page Header ──────────────────────────────────────────────────── -->
    <div class="as_page_header">
        <div>
            <h1 class="as_page_title">Access Studio</h1>
            <p class="as_page_sub">Create restriction rules that override role-based permissions.</p>
        </div>
        <button class="as_btn as_btn_primary" t-on-click="openNew">+ New Access Rule</button>
    </div>

    <!-- ── Filter Bar ───────────────────────────────────────────────────── -->
    <div class="as_filter_bar">
        <div class="as_search_wrap">
            <i class="fa fa-search as_search_icon"/>
            <input class="as_search_input" type="text" placeholder="Search rules..."
                   t-on-input="onSearch"/>
        </div>
        <select class="as_filter_select" t-on-change="onStatusFilter">
            <option value="">All Status ▾</option>
            <option value="active">Active</option>
            <option value="draft">Draft</option>
        </select>
        <select class="as_filter_select" t-on-change="onRiskFilter">
            <option value="">All Risk ▾</option>
            <option value="low">Low</option>
            <option value="medium">Medium</option>
            <option value="high">High</option>
        </select>
    </div>

    <!-- ── Data Table ───────────────────────────────────────────────────── -->
    <div class="as_table">
        <div class="as_table_head">
            <div>RULE</div>
            <div>APPLIES TO</div>
            <div>IMPACT</div>
            <div>STATUS</div>
            <div>RISK</div>
            <div>ACTIONS</div>
        </div>

        <t t-if="this.filteredRules.length === 0">
            <div class="as_empty">No rules match your filters.</div>
        </t>

        <t t-foreach="this.filteredRules" t-as="rule" t-key="rule.id">
            <div class="as_row" t-on-click="() => this.openEdit(rule)">
                <!-- Rule name + sub -->
                <div class="as_cell_rule">
                    <div class="as_rule_name"><t t-esc="rule.name"/></div>
                    <div class="as_rule_sub"><t t-esc="rule.sub"/></div>
                </div>
                <!-- Audience pills -->
                <div class="as_cell_tags">
                    <t t-foreach="rule.audience" t-as="pill" t-key="pill.label">
                        <span t-att-class="'as_pill as_pill_' + pill.type">
                            <t t-esc="pill.label"/>
                        </span>
                    </t>
                </div>
                <!-- Impact badges -->
                <div class="as_cell_tags">
                    <t t-foreach="rule.impact" t-as="badge" t-key="badge.label">
                        <span t-att-class="'as_badge as_badge_' + badge.type">
                            <t t-esc="badge.label"/>
                        </span>
                    </t>
                </div>
                <!-- Status chip -->
                <div>
                    <span t-att-class="'as_status as_status_' + rule.status">
                        <t t-if="rule.status === 'active'">● Active</t>
                        <t t-else="">○ Draft</t>
                    </span>
                </div>
                <!-- Risk chip -->
                <div>
                    <span t-att-class="'as_risk as_risk_' + rule.risk">
                        <t t-esc="rule.risk.charAt(0).toUpperCase() + rule.risk.slice(1)"/>
                    </span>
                </div>
                <!-- Row actions -->
                <div class="as_row_actions">
                    <button t-att-class="'as_row_btn ' + (rule.status === 'active' ? 'as_row_btn_disable' : 'as_row_btn_enable')"
                            t-att-title="rule.status === 'active' ? 'Disable rule' : 'Enable rule'"
                            t-on-click="(ev) => this.toggleRuleStatus(rule, ev)">
                        <i t-att-class="'fa ' + (rule.status === 'active' ? 'fa-pause-circle' : 'fa-play-circle')"/>
                    </button>
                    <button class="as_row_btn as_row_btn_delete" title="Delete rule"
                            t-on-click="(ev) => this.deleteRule(rule, ev)">
                        <i class="fa fa-trash-o"/>
                    </button>
                </div>
            </div>
        </t>
    </div>

    <!-- ── Wizard Modal ──────────────────────────────────────────────────── -->
    <t t-if="this.state.wizardOpen">
        <div class="as_modal_overlay">
            <div class="as_modal">

                <!-- Modal header -->
                <div class="as_modal_head">
                    <div>
                        <div class="as_modal_title">
                            <t t-if="this.state.wizardMode === 'edit'">Edit Rule: <t t-esc="this.state.activeRule.name"/></t>
                            <t t-else="">New Access Rule</t>
                        </div>
                        <div class="as_modal_sub">
                            <t t-if="this.state.wizardMode === 'edit'">Rule overview — use steps above to edit any section</t>
                            <t t-else="">Step <t t-esc="this.state.wizardStep"/> of 4 — <t t-esc="this.wizardStepLabel"/></t>
                        </div>
                    </div>
                    <button class="as_modal_close" t-on-click="closeWizard">
                        <i class="fa fa-times"/>
                    </button>
                </div>

                <!-- Stepper -->
                <div class="as_stepper">
                    <t t-foreach="[1,2,3,4]" t-as="step" t-key="step">
                        <div t-att-class="'as_step as_step_clickable' + (this.state.wizardStep === step ? ' active' : '') + (this.state.wizardStep > step ? ' done' : '')"
                             t-on-click="() => this.goToStep(step)">
                            <div class="as_step_num">
                                <t t-if="this.state.wizardStep > step"><i class="fa fa-check"/></t>
                                <t t-else=""><t t-esc="step"/></t>
                            </div>
                            <div class="as_step_label">
                                <t t-if="step === 1">Define</t>
                                <t t-elif="step === 2">Audience</t>
                                <t t-elif="step === 3">Restrictions</t>
                                <t t-else="">Review</t>
                            </div>
                        </div>
                        <t t-if="step &lt; 4">
                            <div t-att-class="'as_step_line' + (this.state.wizardStep > step ? ' done' : '')"/>
                        </t>
                    </t>
                </div>

                <!-- Modal body (scrollable) -->
                <div class="as_modal_body">

                    <!-- Step 1: Define -->
                    <t t-if="this.state.wizardStep === 1">
                        <div class="as_info_box">
                            Access Studio rules apply <strong>restrictions</strong> on top of role-based permissions.
                        </div>
                        <div class="as_form_group">
                            <label class="as_form_label">Rule Name <span class="as_req">*</span></label>
                            <input class="as_form_input as_wizard_name_input" type="text"
                                   placeholder="e.g. Disable Import for Sales Team"
                                   t-att-value="this.state.wName"
                                   t-on-input="(ev) => { this.state.wName = ev.target.value; }"/>
                        </div>
                        <div class="as_form_group">
                            <label class="as_form_label">Description</label>
                            <textarea class="as_form_input as_textarea" rows="3"
                                      placeholder="What does this rule restrict?"
                                      t-on-input="(ev) => { this.state.wDesc = ev.target.value; }"><t t-esc="this.state.wDesc"/></textarea>
                        </div>
                        <div class="as_form_row">
                            <div class="as_form_group">
                                <label class="as_form_label">Type</label>
                                <select class="as_form_select"
                                        t-on-change="(ev) => { this.state.wType = ev.target.value; }">
                                    <option>Restriction</option>
                                    <option>Readonly</option>
                                    <option>Visibility</option>
                                    <option>Developer Mode</option>
                                </select>
                            </div>
                            <div class="as_form_group">
                                <label class="as_form_label">Priority</label>
                                <select class="as_form_select"
                                        t-on-change="(ev) => { this.state.wPriority = ev.target.value; }">
                                    <option>Normal (50)</option>
                                    <option>High (100)</option>
                                    <option>Low (10)</option>
                                </select>
                            </div>
                        </div>
                        <div class="as_toggle_row">
                            <div>
                                <div class="as_toggle_label">Activate immediately</div>
                                <div class="as_toggle_sub">Applies on publish</div>
                            </div>
                            <label class="as_toggle_switch">
                                <input type="checkbox"
                                       t-att-checked="this.state.wActivate ? 'checked' : undefined"
                                       t-on-change="(ev) => { this.state.wActivate = ev.target.checked; }"/>
                                <span class="as_toggle_track"/>
                                <span class="as_toggle_thumb"/>
                            </label>
                        </div>
                    </t>

                    <!-- Step 2: Audience -->
                    <t t-elif="this.state.wizardStep === 2">
                        <div class="as_info_box">
                            <strong>Who should this rule restrict?</strong> You can target roles, departments, companies, AND/OR specific individual users.
                        </div>

                        <!-- Roles -->
                        <div class="as_form_group">
                            <label class="as_form_label">Apply to Roles</label>
                            <div class="as_tokenizer_wrap">
                                <div class="as_tokenizer" t-on-click.stop="() => this.openDropdown('roles')">
                                    <t t-foreach="this.state.wRoles" t-as="item" t-key="item.id">
                                        <span class="as_tag as_tag_role">
                                            <t t-esc="item.name"/>
                                            <span class="as_tag_remove" t-on-mousedown.prevent.stop="() => this.removeWItem('roles', item.id)">×</span>
                                        </span>
                                    </t>
                                    <input type="text" class="as_tokenizer_input"
                                           t-att-placeholder="this.state.wRoles.length ? '' : 'Search roles...'"
                                           t-att-value="this.state.wSearches.roles"
                                           t-on-input="(ev) => this.onTokenizerInput(ev, 'roles')"
                                           t-on-keydown="(ev) => this.onTokenizerKeydown(ev, 'roles')"
                                           t-on-focus="() => this.openDropdown('roles')"
                                           t-on-blur="() => this.scheduleClose('roles')"/>
                                </div>
                                <div t-if="this.state.wOpenField === 'roles'" class="as_dropdown">
                                    <t t-foreach="this.getFiltered('roles')" t-as="opt" t-key="opt.id">
                                        <div class="as_dropdown_item" t-on-mousedown.prevent="() => this.selectOption('roles', opt)">
                                            <t t-esc="opt.name"/>
                                        </div>
                                    </t>
                                    <div t-if="!this.getFiltered('roles').length" class="as_dropdown_empty">No results found.</div>
                                </div>
                            </div>
                        </div>

                        <!-- Departments -->
                        <div class="as_form_group">
                            <label class="as_form_label">Apply to Departments</label>
                            <div class="as_tokenizer_wrap">
                                <div class="as_tokenizer" t-on-click.stop="() => this.openDropdown('depts')">
                                    <t t-foreach="this.state.wDepts" t-as="item" t-key="item.id">
                                        <span class="as_tag as_tag_dept">
                                            <t t-esc="item.name"/>
                                            <span class="as_tag_remove" t-on-mousedown.prevent.stop="() => this.removeWItem('depts', item.id)">×</span>
                                        </span>
                                    </t>
                                    <input type="text" class="as_tokenizer_input"
                                           t-att-placeholder="this.state.wDepts.length ? '' : 'Search departments...'"
                                           t-att-value="this.state.wSearches.depts"
                                           t-on-input="(ev) => this.onTokenizerInput(ev, 'depts')"
                                           t-on-keydown="(ev) => this.onTokenizerKeydown(ev, 'depts')"
                                           t-on-focus="() => this.openDropdown('depts')"
                                           t-on-blur="() => this.scheduleClose('depts')"/>
                                </div>
                                <div t-if="this.state.wOpenField === 'depts'" class="as_dropdown">
                                    <t t-foreach="this.getFiltered('depts')" t-as="opt" t-key="opt.id">
                                        <div class="as_dropdown_item" t-on-mousedown.prevent="() => this.selectOption('depts', opt)">
                                            <t t-esc="opt.name"/>
                                        </div>
                                    </t>
                                    <div t-if="!this.getFiltered('depts').length" class="as_dropdown_empty">No results found.</div>
                                </div>
                            </div>
                        </div>

                        <!-- Specific Users -->
                        <div class="as_form_group">
                            <label class="as_form_label">Apply to Specific Users <span class="as_form_label_note">— for user-level restrictions</span></label>
                            <div class="as_tokenizer_wrap">
                                <div class="as_tokenizer" t-on-click.stop="() => this.openDropdown('users')">
                                    <t t-foreach="this.state.wUsers" t-as="item" t-key="item.id">
                                        <span class="as_tag as_tag_user">
                                            <t t-esc="item.name"/>
                                            <span class="as_tag_remove" t-on-mousedown.prevent.stop="() => this.removeWItem('users', item.id)">×</span>
                                        </span>
                                    </t>
                                    <input type="text" class="as_tokenizer_input"
                                           t-att-placeholder="this.state.wUsers.length ? '' : 'Search users...'"
                                           t-att-value="this.state.wSearches.users"
                                           t-on-input="(ev) => this.onTokenizerInput(ev, 'users')"
                                           t-on-keydown="(ev) => this.onTokenizerKeydown(ev, 'users')"
                                           t-on-focus="() => this.openDropdown('users')"
                                           t-on-blur="() => this.scheduleClose('users')"/>
                                </div>
                                <div t-if="this.state.wOpenField === 'users'" class="as_dropdown">
                                    <t t-foreach="this.getFiltered('users')" t-as="opt" t-key="opt.id">
                                        <div class="as_dropdown_item" t-on-mousedown.prevent="() => this.selectOption('users', opt)">
                                            <t t-esc="opt.name"/>
                                        </div>
                                    </t>
                                    <div t-if="!this.getFiltered('users').length" class="as_dropdown_empty">No results found.</div>
                                </div>
                            </div>
                        </div>

                        <!-- Companies -->
                        <div class="as_form_group">
                            <label class="as_form_label">Apply to Companies</label>
                            <div class="as_tokenizer_wrap">
                                <div class="as_tokenizer" t-on-click.stop="() => this.openDropdown('companies')">
                                    <t t-foreach="this.state.wCompanies" t-as="item" t-key="item.id">
                                        <span class="as_tag as_tag_company">
                                            <t t-esc="item.name"/>
                                            <span class="as_tag_remove" t-on-mousedown.prevent.stop="() => this.removeWItem('companies', item.id)">×</span>
                                        </span>
                                    </t>
                                    <input type="text" class="as_tokenizer_input"
                                           t-att-placeholder="this.state.wCompanies.length ? '' : 'Search companies...'"
                                           t-att-value="this.state.wSearches.companies"
                                           t-on-input="(ev) => this.onTokenizerInput(ev, 'companies')"
                                           t-on-keydown="(ev) => this.onTokenizerKeydown(ev, 'companies')"
                                           t-on-focus="() => this.openDropdown('companies')"
                                           t-on-blur="() => this.scheduleClose('companies')"/>
                                </div>
                                <div t-if="this.state.wOpenField === 'companies'" class="as_dropdown">
                                    <t t-foreach="this.getFiltered('companies')" t-as="opt" t-key="opt.id">
                                        <div class="as_dropdown_item" t-on-mousedown.prevent="() => this.selectOption('companies', opt)">
                                            <t t-esc="opt.name"/>
                                        </div>
                                    </t>
                                    <div t-if="!this.getFiltered('companies').length" class="as_dropdown_empty">No results found.</div>
                                </div>
                            </div>
                        </div>

                        <!-- Exclude Users -->
                        <div class="as_form_group">
                            <label class="as_form_label">Exclude Users <span class="as_form_label_note">— these users won't be affected</span></label>
                            <div class="as_tokenizer_wrap">
                                <div class="as_tokenizer" t-on-click.stop="() => this.openDropdown('exclude')">
                                    <t t-foreach="this.state.wExclude" t-as="item" t-key="item.id">
                                        <span class="as_tag as_tag_exclude">
                                            <t t-esc="item.name"/>
                                            <span class="as_tag_remove" t-on-mousedown.prevent.stop="() => this.removeWItem('exclude', item.id)">×</span>
                                        </span>
                                    </t>
                                    <input type="text" class="as_tokenizer_input"
                                           t-att-placeholder="this.state.wExclude.length ? '' : 'Search users to exclude...'"
                                           t-att-value="this.state.wSearches.exclude"
                                           t-on-input="(ev) => this.onTokenizerInput(ev, 'exclude')"
                                           t-on-keydown="(ev) => this.onTokenizerKeydown(ev, 'exclude')"
                                           t-on-focus="() => this.openDropdown('exclude')"
                                           t-on-blur="() => this.scheduleClose('exclude')"/>
                                </div>
                                <div t-if="this.state.wOpenField === 'exclude'" class="as_dropdown">
                                    <t t-foreach="this.getFiltered('exclude')" t-as="opt" t-key="opt.id">
                                        <div class="as_dropdown_item" t-on-mousedown.prevent="() => this.selectOption('exclude', opt)">
                                            <t t-esc="opt.name"/>
                                        </div>
                                    </t>
                                    <div t-if="!this.getFiltered('exclude').length" class="as_dropdown_empty">No results found.</div>
                                </div>
                            </div>
                        </div>

                        <!-- Impact estimate -->
                        <div class="as_impact_box">
                            <strong>Estimated impact:</strong>
                            <t t-if="this.state.wRoles.length + this.state.wUsers.length > 0">
                                <t t-if="this.state.wRoles.length > 0">
                                    Users via <t t-esc="this.state.wRoles.map(r => r.name).join(', ')"/> role(s)
                                </t>
                                <t t-if="this.state.wUsers.length > 0">
                                    + <t t-esc="this.state.wUsers.length"/> direct user(s)
                                </t>
                            </t>
                            <t t-else="">No audience selected yet.</t>
                        </div>
                    </t>

                    <!-- Step 3: Restrictions -->
                    <t t-elif="this.state.wizardStep === 3">
                        <div class="as_info_box as_info_box_accent">
                            Configure what this rule restricts. Use the tabs below — each controls a different category. Only configure the tabs you need.
                        </div>
                        <!-- Restriction tabs -->
                        <div class="as_rtabs">
                            <t t-foreach="[
                                {id:'menu',    label:'Hide Menu'},
                                {id:'model',   label:'Model Access'},
                                {id:'field',   label:'Field Access'},
                                {id:'domain',  label:'Domain Access'},
                                {id:'button',  label:'Button/Tab'},
                                {id:'filter',  label:'Filter/Group By'},
                                {id:'chatter', label:'Chatter'},
                                {id:'global',  label:'Global'}
                            ]" t-as="tab" t-key="tab.id">
                                <button t-att-class="'as_rtab' + (this.state.wRestrictTab === tab.id ? ' active' : '')"
                                        t-on-click="() => this.setRestrictTab(tab.id)">
                                    <t t-esc="tab.label"/>
                                </button>
                            </t>
                        </div>

                        <!-- Hide Menu -->
                        <t t-if="this.state.wRestrictTab === 'menu'">
                            <p class="as_rtab_desc">Search and select menus to hide from the selected audience.</p>
                            <div class="as_form_group">
                                <div class="as_tokenizer_wrap as_big_selector">
                                    <div class="as_tokenizer" t-on-click.stop="() => this.openDropdown('menus')">
                                        <t t-foreach="this.state.wHideMenus" t-as="item" t-key="item.id">
                                            <span class="as_tag as_tag_danger">
                                                <t t-esc="item.name"/>
                                                <span class="as_tag_remove" t-on-mousedown.prevent.stop="() => this.removeWItem('menus', item.id)">×</span>
                                            </span>
                                        </t>
                                        <input type="text" class="as_tokenizer_input"
                                               t-att-placeholder="this.state.wHideMenus.length ? '' : 'Search menus… e.g. Accounting, HR, Fleet'"
                                               t-att-value="this.state.wSearches.menus"
                                               t-on-input="(ev) => this.onTokenizerInput(ev, 'menus')"
                                               t-on-keydown="(ev) => this.onTokenizerKeydown(ev, 'menus')"
                                               t-on-focus="() => this.openDropdown('menus')"
                                               t-on-blur="() => this.scheduleClose('menus')"/>
                                    </div>
                                    <div t-if="this.state.wOpenField === 'menus'" class="as_dropdown as_selector_panel" t-on-mousedown.stop="">
                                        <div class="as_selector_panel_head">
                                            <div>
                                                <div class="as_selector_panel_title">Menu catalog</div>
                                                <div class="as_selector_panel_sub">
                                                    <t t-esc="this.state.wHideMenus.length"/> selected · <t t-esc="this.getFiltered('menus').length"/> visible
                                                </div>
                                            </div>
                                            <button type="button" class="as_selector_close" t-on-mousedown.prevent.stop="() => this.closeDropdown()">
                                                <i class="fa fa-times"/>
                                            </button>
                                        </div>
                                        <t t-foreach="this.getFiltered('menus')" t-as="opt" t-key="opt.id">
                                            <div class="as_dropdown_item" t-on-mousedown.prevent="() => this.selectOption('menus', opt)">
                                                <i class="fa fa-bars as_dropdown_item_icon"/>
                                                <t t-esc="opt.name"/>
                                            </div>
                                        </t>
                                        <div t-if="!this.getFiltered('menus').length" class="as_dropdown_empty">No results found.</div>
                                    </div>
                                </div>
                            </div>
                        </t>

                        <!-- Model Access -->
                        <t t-elif="this.state.wRestrictTab === 'model'">
                            <p class="as_rtab_desc">Select models to restrict. Each selected model can be configured with operation-level access rules.</p>
                            <div class="as_model_access_builder">
                                <t t-if="this.state.wModelItems.length">
                                    <div class="as_model_rules">
                                        <t t-foreach="this.state.wModelItems" t-as="model" t-key="model.id">
                                            <div t-att-class="'as_model_rule_card' + (this.isModelCardOpen(model.id) ? ' open' : '')">
                                                <div class="as_model_rule_head" t-on-click="() => this.toggleModelCard(model.id)">
                                                    <div class="as_model_rule_icon"><i class="fa fa-cube"/></div>
                                                    <div>
                                                        <div class="as_model_rule_title"><t t-esc="model.name"/></div>
                                                        <div class="as_model_rule_sub"><t t-esc="model.model || 'Selected model'"/> · <t t-esc="this.modelRestrictionCount(model)"/> restrictions</div>
                                                    </div>
                                                    <span t-att-class="'as_model_rule_badge' + (this.modelRestrictionCount(model) ? '' : ' empty')">
                                                        <t t-esc="this.modelRestrictionCount(model)"/> restricted
                                                    </span>
                                                    <div class="as_model_rule_presets">
                                                        <button type="button" class="as_model_preset_badge" t-on-click.stop="() => this.setModelPreset(model.id, 'all')">Block all</button>
                                                        <button type="button" class="as_model_preset_badge clear" t-on-click.stop="() => this.setModelPreset(model.id, 'clear')">Clear all</button>
                                                        <button type="button" class="as_model_remove_btn" title="Remove" t-on-click.stop="() => this.removeWItem('models', model.id)">
                                                            <i class="fa fa-times"/>
                                                        </button>
                                                    </div>
                                                    <i class="fa fa-chevron-down as_model_rule_chevron"/>
                                                </div>
                                                <t t-if="this.isModelCardOpen(model.id)">
                                                    <div class="as_model_rule_grid">
                                                        <t t-foreach="this.modelAccessOptions" t-as="opt" t-key="opt.key">
                                                            <label t-att-class="'as_model_toggle_row' + (model[opt.key] ? ' active' : '')">
                                                                <span><t t-esc="opt.label"/></span>
                                                                <input type="checkbox"
                                                                       t-att-checked="model[opt.key]"
                                                                       t-on-change="() => this.toggleModelAccess(model.id, opt.key)"/>
                                                                <span class="as_switch_track"><span class="as_switch_thumb"/></span>
                                                            </label>
                                                        </t>
                                                    </div>
                                                </t>
                                            </div>
                                        </t>
                                    </div>
                                </t>
                                <t t-else="">
                                    <div class="as_model_placeholder">
                                        <i class="fa fa-cube"/> Click Add Model to create a model restriction section.
                                    </div>
                                </t>
                                <div class="as_model_add_bar">
                                    <div class="as_model_pick_row">
                                        <div class="as_tokenizer as_model_pick_box" t-on-click.stop="() => this.openDropdown('models')">
                                            <t t-foreach="this.state.wModelCandidates" t-as="candidate" t-key="candidate.id">
                                                <span class="as_tag as_tag_role as_model_pick_tag">
                                                    <t t-esc="candidate.name"/>
                                                    <span class="as_tag_remove" t-on-mousedown.prevent.stop="() => this.removePendingModel(candidate.id)">×</span>
                                                </span>
                                            </t>
                                            <input type="text"
                                                   class="as_model_pick_input"
                                                   placeholder="Search models... e.g. Journal Entry, account.move"
                                                   t-att-value="this.state.wSearches.models"
                                                   t-on-input="(ev) => this.onTokenizerInput(ev, 'models')"
                                                   t-on-keydown="(ev) => this.onTokenizerKeydown(ev, 'models')"
                                                   t-on-focus="() => this.openDropdown('models')"
                                                   t-on-blur="() => this.scheduleClose('models')"/>
                                        </div>
                                        <button type="button"
                                                t-att-class="'as_model_add_btn' + (this.modelAccessPendingCount ? '' : ' disabled')"
                                                t-on-mousedown.prevent.stop="addSelectedModelAccess">Add Model</button>
                                    </div>
                                </div>
                                <div class="as_model_catalog_wrap">
                                    <div t-if="this.state.wOpenField === 'models'" class="as_dropdown as_selector_panel" t-on-mousedown.stop="">
                                        <t t-foreach="this.getFiltered('models')" t-as="opt" t-key="opt.id">
                                            <div t-att-class="'as_dropdown_item'" t-on-mousedown.prevent="() => this.selectOption('models', opt)">
                                                <i class="fa fa-cube as_dropdown_item_icon"/>
                                                <span class="as_dropdown_item_text">
                                                    <span class="as_dropdown_item_title" t-esc="opt.name"/>
                                                    <span t-if="opt.model" class="as_dropdown_item_sub" t-esc="opt.model"/>
                                                </span>
                                            </div>
                                        </t>
                                        <div t-if="!this.getFiltered('models').length" class="as_dropdown_empty">No results found.</div>
                                    </div>
                                </div>
                            </div>
                        </t>

                        <!-- Field Access -->
                        <t t-elif="this.state.wRestrictTab === 'field'">
                            <p class="as_rtab_desc">Hide fields, make them read-only or required, or remove external links for the selected audience.</p>
                            <div class="as_field_builder">
                                <t t-if="this.state.wFieldModels.length">
                                    <div class="as_field_rules">
                                        <t t-foreach="this.state.wFieldModels" t-as="model" t-key="model.id">
                                            <div t-att-class="'as_field_model_card' + (this.isFieldModelCardOpen(model.id) ? ' open' : '')">
                                                <div class="as_model_rule_head as_field_model_head" t-on-click="() => this.toggleFieldModelCard(model)">
                                                    <div class="as_model_rule_icon as_field_icon">
                                                        <i class="fa fa-list"/>
                                                    </div>
                                                    <div>
                                                        <div class="as_model_rule_title" t-esc="model.name"/>
                                                        <div class="as_model_rule_sub">
                                                            <t t-esc="model.model"/> · <t t-esc="this.fieldRuleCountForModel(model.id)"/> fields configured
                                                        </div>
                                                    </div>
                                                    <span t-att-class="'as_model_rule_badge' + (this.fieldRuleCountForModel(model.id) ? '' : ' empty')">
                                                        <t t-esc="this.fieldRuleCountForModel(model.id)"/> fields
                                                    </span>
                                                    <button type="button" class="as_model_remove_btn" title="Remove" t-on-click.stop="() => this.removeFieldModelSection(model.id)">
                                                        <i class="fa fa-times"/>
                                                    </button>
                                                    <i class="fa fa-chevron-down as_model_rule_chevron"/>
                                                </div>
                                                <t t-if="this.isFieldModelCardOpen(model.id)">
                                                    <div class="as_field_model_body">
                                                        <t t-if="this.fieldRulesForModel(model.id).length">
                                                            <div class="as_field_matrix">
                                                                <div class="as_field_matrix_head">
                                                                    <div>Field</div>
                                                                    <div>Hide</div>
                                                                    <div>Readonly</div>
                                                                    <div>Required</div>
                                                                </div>
                                                                <t t-foreach="this.fieldRulesForModel(model.id)" t-as="rule" t-key="rule.id">
                                                                    <div class="as_field_matrix_row">
                                                                        <div class="as_field_matrix_field">
                                                                            <div class="as_model_rule_title" t-esc="this._fieldRuleFieldNames(rule)"/>
                                                                            <div class="as_model_rule_sub">
                                                                                <t t-esc="this._fieldRuleTechnicalNames(rule)"/>
                                                                            </div>
                                                                        </div>
                                                                        <label class="as_field_check">
                                                                            <input type="checkbox" t-att-checked="rule.invisible" t-on-change="(ev) => this.setFieldRuleAttribute(rule.id, 'invisible', ev.target.checked)"/>
                                                                            <span></span>
                                                                        </label>
                                                                        <label class="as_field_check">
                                                                            <input type="checkbox" t-att-checked="rule.readonly" t-on-change="(ev) => this.setFieldRuleAttribute(rule.id, 'readonly', ev.target.checked)"/>
                                                                            <span></span>
                                                                        </label>
                                                                        <label class="as_field_check">
                                                                            <input type="checkbox" t-att-checked="rule.required" t-on-change="(ev) => this.setFieldRuleAttribute(rule.id, 'required', ev.target.checked)"/>
                                                                            <span></span>
                                                                        </label>
                                                                    </div>
                                                                </t>
                                                            </div>
                                                        </t>
                                                        <t t-else="">
                                                            <div class="as_model_placeholder as_field_empty">
                                                                <i class="fa fa-list"/> No fields configured yet.
                                                            </div>
                                                        </t>

                                                        <div class="as_field_add_bar">
                                                            <div class="as_model_pick_row as_field_pick_row">
                                                                <div class="as_tokenizer as_model_pick_box" t-on-click.stop="() => this.openFieldPickerForModel(model)">
                                                                    <t t-foreach="this.state.wFieldItems" t-as="item" t-key="item.id">
                                                                        <span class="as_tag as_tag_user as_model_pick_tag">
                                                                            <t t-esc="item.name"/>
                                                                            <span class="as_tag_remove" t-on-mousedown.prevent.stop="() => this.removeWItem('field_fields', item.id)">×</span>
                                                                        </span>
                                                                    </t>
                                                                    <input type="text"
                                                                           class="as_model_pick_input"
                                                                           t-att-placeholder="'Search fields... e.g. customer, journal_id, amount'"
                                                                           t-att-value="this.state.wSearches.field_fields"
                                                                           t-on-input="(ev) => this.onTokenizerInput(ev, 'field_fields')"
                                                                           t-on-keydown="(ev) => this.onTokenizerKeydown(ev, 'field_fields')"
                                                                           t-on-focus="() => this.openFieldPickerForModel(model)"
                                                                           t-on-blur="() => this.scheduleClose('field_fields')"/>
                                                                </div>
                                                                <button type="button"
                                                                        t-att-class="'as_model_add_btn' + (this.state.wFieldItems.length ? '' : ' disabled')"
                                                                        t-on-mousedown.prevent.stop="addFieldRule">Add Field</button>
                                                                <div t-if="this.state.wOpenField === 'field_fields' &amp;&amp; this.isFieldModelCardOpen(model.id)" class="as_dropdown as_selector_panel as_field_dropdown" t-on-mousedown.stop="">
                                                                    <t t-foreach="this.getFiltered('field_fields')" t-as="opt" t-key="opt.id">
                                                                        <div class="as_dropdown_item" t-on-mousedown.prevent="() => this.selectOption('field_fields', opt)">
                                                                            <i class="fa fa-list as_dropdown_item_icon"/>
                                                                            <span class="as_dropdown_item_text">
                                                                                <span class="as_dropdown_item_title" t-esc="opt.name"/>
                                                                                <span class="as_dropdown_item_sub"><t t-esc="opt.field_name"/> · <t t-esc="opt.ttype"/></span>
                                                                            </span>
                                                                        </div>
                                                                    </t>
                                                                    <div t-if="!this.getFiltered('field_fields').length" class="as_dropdown_empty">No results found.</div>
                                                                </div>
                                                            </div>
                                                        </div>
                                                    </div>
                                                </t>
                                            </div>
                                        </t>
                                    </div>
                                </t>
                                <t t-else="">
                                    <div class="as_model_placeholder">
                                        <i class="fa fa-list"/> Click Add Model to create a field restriction section.
                                    </div>
                                </t>
                                <div class="as_model_add_bar">
                                    <div class="as_model_pick_row">
                                        <div class="as_tokenizer as_model_pick_box" t-on-click.stop="() => this.openDropdown('field_models')">
                                            <t t-foreach="this.state.wFieldModelCandidates" t-as="candidate" t-key="candidate.id">
                                                <span class="as_tag as_tag_role as_model_pick_tag">
                                                    <t t-esc="candidate.name"/>
                                                    <span class="as_tag_remove" t-on-mousedown.prevent.stop="() => this.removePendingFieldModel(candidate.id)">×</span>
                                                </span>
                                            </t>
                                            <input type="text"
                                                   class="as_model_pick_input"
                                                   placeholder="Search models... e.g. Journal Entry, account.move"
                                                   t-att-value="this.state.wSearches.field_models"
                                                   t-on-input="(ev) => this.onTokenizerInput(ev, 'field_models')"
                                                   t-on-keydown="(ev) => this.onTokenizerKeydown(ev, 'field_models')"
                                                   t-on-focus="() => this.openDropdown('field_models')"
                                                   t-on-blur="() => this.scheduleClose('field_models')"/>
                                        </div>
                                        <button type="button"
                                                t-att-class="'as_model_add_btn' + (this.state.wFieldModelCandidates.length ? '' : ' disabled')"
                                                t-on-click.stop="addSelectedFieldModels">Add Model</button>
                                    </div>
                                </div>
                                <div class="as_model_catalog_wrap">
                                    <div t-if="this.state.wOpenField === 'field_models'" class="as_dropdown as_selector_panel" t-on-mousedown.stop="">
                                        <t t-foreach="this.getFilteredFieldModels()" t-as="model" t-key="model.id">
                                            <div class="as_dropdown_item" t-on-mousedown.prevent="() => this.selectOption('field_models', model)">
                                                <i class="fa fa-cube as_dropdown_item_icon"/>
                                                <span class="as_dropdown_item_text">
                                                    <span class="as_dropdown_item_title" t-esc="model.name"/>
                                                    <span class="as_dropdown_item_sub" t-esc="model.model"/>
                                                </span>
                                            </div>
                                        </t>
                                        <div t-if="!this.getFilteredFieldModels().length" class="as_dropdown_empty">No results found.</div>
                                    </div>
                                </div>
                            </div>

                        </t>

                        <!-- Domain Access -->
                        <t t-elif="this.state.wRestrictTab === 'domain'">
                            <p class="as_rtab_desc">Apply strict model rights and record-level domains. These rules affect backend, code, and API access.</p>
                            <div class="as_domain_builder">
                                <t t-if="this.state.wDomainRules.length">
                                    <div class="as_domain_rules">
                                        <t t-foreach="this.state.wDomainRules" t-as="rule" t-key="rule.id">
                                            <div t-att-class="'as_domain_rule_card' + (this.isDomainCardOpen(rule.id) ? ' open' : '')">
                                                <div class="as_model_rule_head as_domain_rule_head" t-on-click="() => this.toggleDomainCard(rule.id)">
                                                    <div class="as_model_rule_icon as_domain_icon">
                                                        <i class="fa fa-filter"/>
                                                    </div>
                                                    <div>
                                                        <div class="as_model_rule_title" t-esc="rule.name"/>
                                                        <div class="as_model_rule_sub">
                                                            <t t-esc="rule.model"/> · domain
                                                        </div>
                                                    </div>
                                                    <span t-att-class="'as_model_rule_badge' + (rule.apply_domain ? '' : ' empty')">
                                                        <t t-esc="rule.apply_domain ? 'Domain on' : 'Domain off'"/>
                                                    </span>
                                                    <button type="button" class="as_model_remove_btn" t-on-click.stop="() => this.removeDomainRule(rule.id)" title="Remove">
                                                        <i class="fa fa-times"/>
                                                    </button>
                                                    <i class="fa fa-chevron-down as_model_rule_chevron"/>
                                                </div>
                                                <t t-if="this.isDomainCardOpen(rule.id)">
                                                    <div class="as_domain_rule_body">
                                                        <label class="as_domain_apply">
                                                            <input type="checkbox" t-att-checked="rule.apply_domain" t-on-change="() => this.toggleDomainRight(rule.id, 'apply_domain')"/>
                                                            <span>Apply custom domain filter</span>
                                                        </label>
                                                        <textarea class="as_domain_textarea"
                                                                  t-att-disabled="!rule.apply_domain"
                                                                  t-att-value="rule.domain"
                                                                  t-on-input="(ev) => this.setDomainValue(rule.id, ev.target.value)"
                                                                  placeholder="[] or [('user_id', '=', user.id)]"/>
                                                    </div>
                                                </t>
                                            </div>
                                        </t>
                                    </div>
                                </t>
                                <t t-else="">
                                    <div class="as_model_placeholder">
                                        <i class="fa fa-filter"/> Click Add Domain Rule to create a domain section.
                                    </div>
                                </t>
                                <div class="as_domain_add_bar">
                                    <t t-if="this.state.wDomainPickerOpen || this.state.wDomainCandidates.length">
                                        <div class="as_model_pick_row">
                                            <div class="as_tokenizer as_model_pick_box" t-on-click.stop="() => this.openDropdown('domain_models')">
                                                <t t-foreach="this.state.wDomainCandidates" t-as="candidate" t-key="candidate.id">
                                                    <span class="as_tag as_tag_role as_model_pick_tag">
                                                        <t t-esc="candidate.name"/>
                                                        <span class="as_tag_remove" t-on-mousedown.prevent.stop="() => this.removePendingDomainModel(candidate.id)">×</span>
                                                    </span>
                                                </t>
                                                <input type="text"
                                                       class="as_model_pick_input"
                                                       placeholder="Search models... e.g. Customer, res.partner"
                                                       t-att-value="this.state.wSearches.domain_models"
                                                       t-on-input="(ev) => this.onTokenizerInput(ev, 'domain_models')"
                                                       t-on-keydown="(ev) => this.onTokenizerKeydown(ev, 'domain_models')"
                                                       t-on-focus="() => this.openDropdown('domain_models')"
                                                       t-on-blur="() => this.scheduleClose('domain_models')"/>
                                            </div>
                                            <button type="button"
                                                    t-att-class="'as_model_add_btn' + (this.state.wDomainCandidates.length ? '' : ' disabled')"
                                                    t-on-mousedown.prevent.stop="() => this.addDomainRule(this.state.wDomainCandidates)">Add Domain Rule</button>
                                        </div>
                                        <div class="as_model_catalog_wrap">
                                            <div t-if="this.state.wOpenField === 'domain_models'" class="as_dropdown as_selector_panel" t-on-mousedown.stop="">
                                                <t t-foreach="this.getFiltered('domain_models')" t-as="model" t-key="model.id">
                                                    <div class="as_dropdown_item" t-on-mousedown.prevent="() => this.selectOption('domain_models', model)">
                                                        <i class="fa fa-filter as_dropdown_item_icon"/>
                                                        <span class="as_dropdown_item_text">
                                                            <span class="as_dropdown_item_title" t-esc="model.name"/>
                                                            <span class="as_dropdown_item_sub" t-esc="model.model"/>
                                                        </span>
                                                    </div>
                                                </t>
                                                <div t-if="!this.getFiltered('domain_models').length" class="as_dropdown_empty">No results found.</div>
                                            </div>
                                        </div>
                                    </t>
                                    <t t-else="">
                                        <button type="button"
                                                class="as_model_add_btn"
                                                t-on-click.stop="() => this.addDomainRule()">+ Add Domain Rule</button>
                                    </t>
                                </div>
                            </div>
                        </t>

                        <!-- Button/Tab -->
                        <t t-elif="this.state.wRestrictTab === 'button'">
                            <p class="as_rtab_desc">Hide action buttons, object buttons, notebook tabs, smart buttons, and kanban links for selected models.</p>
                            <div class="as_button_builder">
                                <t t-if="this.state.wButtonRules.length">
                                    <div class="as_button_rules">
                                        <t t-foreach="this.state.wButtonRules" t-as="rule" t-key="rule.id">
                                            <div t-att-class="'as_button_rule_card' + (this.isButtonModelCardOpen(rule.id) ? ' open' : '')">
                                                <div class="as_model_rule_head as_button_rule_head" t-on-click="() => this.toggleButtonModelCard(rule)">
                                                    <div class="as_model_rule_icon as_button_icon">
                                                        <i class="fa fa-toggle-off"/>
                                                    </div>
                                                    <div>
                                                        <div class="as_model_rule_title" t-esc="rule.model_name"/>
                                                        <div class="as_model_rule_sub">
                                                            <t t-esc="rule.model"/> · <t t-esc="rule.nodes.length"/> items hidden
                                                        </div>
                                                    </div>
                                                    <span t-att-class="'as_model_rule_badge' + (rule.nodes.length ? '' : ' empty')">
                                                        <t t-esc="rule.nodes.length"/> items
                                                    </span>
                                                    <button type="button" class="as_model_remove_btn" t-on-click.stop="() => this.removeButtonRule(rule.id)" title="Remove">
                                                        <i class="fa fa-times"/>
                                                    </button>
                                                    <i class="fa fa-chevron-down as_model_rule_chevron"/>
                                                </div>
                                                <t t-if="this.isButtonModelCardOpen(rule.id)">
                                                    <div class="as_button_rule_body">
                                                        <div class="as_form_group">
                                                            <label class="as_form_label">Buttons, Tabs, Links</label>
                                                            <div class="as_tokenizer_wrap as_big_selector">
                                                                <div t-att-class="'as_tokenizer' + (this.state.wButtonModel ? '' : ' disabled')" t-on-click.stop="() => this.state.wButtonModel &amp;&amp; this.openDropdown('button_nodes')">
                                                                    <input type="text" class="as_tokenizer_input"
                                                                           t-att-disabled="!this.state.wButtonModel"
                                                                           t-att-placeholder="this.state.wButtonModel ? 'Search buttons/tabs... e.g. Post, Notes, Reverse Entry' : 'Open the card first'"
                                                                           t-att-value="this.state.wSearches.button_nodes"
                                                                           t-on-input="(ev) => this.onTokenizerInput(ev, 'button_nodes')"
                                                                           t-on-focus="() => this.state.wButtonModel &amp;&amp; this.openDropdown('button_nodes')"
                                                                           t-on-blur="() => this.scheduleClose('button_nodes')"/>
                                                                </div>
                                                                <div t-if="this.state.wOpenField === 'button_nodes'" class="as_dropdown as_selector_panel" t-on-mousedown.stop="">
                                                                    <div class="as_selector_panel_head">
                                                                        <div>
                                                                            <div class="as_selector_panel_title">Available View Nodes</div>
                                                                            <div class="as_selector_panel_sub">Select an item to hide · <t t-esc="this.getFiltered('button_nodes').length"/> visible</div>
                                                                        </div>
                                                                        <button type="button" class="as_selector_close" t-on-mousedown.prevent.stop="() => this.closeDropdown()">
                                                                            <i class="fa fa-times"/>
                                                                        </button>
                                                                    </div>
                                                                    <t t-foreach="this.getFiltered('button_nodes')" t-as="node" t-key="node.id">
                                                                        <div class="as_dropdown_item" t-on-mousedown.prevent="() => this.selectOption('button_nodes', node)">
                                                                            <i t-att-class="node.node_type === 'page' ? 'fa fa-columns as_dropdown_item_icon' : (node.node_type === 'link' ? 'fa fa-link as_dropdown_item_icon' : 'fa fa-toggle-off as_dropdown_item_icon')"/>
                                                                            <span class="as_dropdown_item_text">
                                                                                <span class="as_dropdown_item_title">
                                                                                    <t t-esc="node.name"/>
                                                                                    <t t-if="node.is_smart_button"> · Smart</t>
                                                                                </span>
                                                                                <span class="as_dropdown_item_sub">
                                                                                    <t t-esc="node.node_type"/> · <t t-esc="node.attribute_name"/>
                                                                                </span>
                                                                            </span>
                                                                        </div>
                                                                    </t>
                                                                    <div t-if="!this.getFiltered('button_nodes').length" class="as_dropdown_empty">No results found.</div>
                                                                </div>
                                                            </div>
                                                        </div>

                                                        <div t-if="rule.nodes.length" class="as_button_node_grid">
                                                            <t t-foreach="rule.nodes" t-as="node" t-key="node.id">
                                                                <span t-att-class="'as_button_node_chip ' + node.node_type">
                                                                    <i t-att-class="node.node_type === 'page' ? 'fa fa-columns' : (node.node_type === 'link' ? 'fa fa-link' : 'fa fa-toggle-off')"/>
                                                                    <span>
                                                                        <t t-esc="node.name"/>
                                                                        <small t-esc="node.node_type"/>
                                                                    </span>
                                                                    <button type="button" t-on-click="() => this.removeButtonNode(rule.id, node.id)">×</button>
                                                                </span>
                                                            </t>
                                                        </div>
                                                    </div>
                                                </t>
                                            </div>
                                        </t>
                                    </div>
                                </t>
                                <t t-else="">
                                    <div class="as_model_placeholder">
                                        <i class="fa fa-toggle-off"/> Click Add Model Section to create a button/tab section.
                                    </div>
                                </t>
                                <div class="as_button_add_bar">
                                    <t t-if="this.state.wButtonPickerOpen || this.state.wButtonModelCandidates.length">
                                        <div class="as_model_pick_row">
                                            <div class="as_tokenizer as_model_pick_box" t-on-click.stop="() => this.openDropdown('button_models')">
                                                <t t-foreach="this.state.wButtonModelCandidates" t-as="candidate" t-key="candidate.id">
                                                    <span class="as_tag as_tag_role as_model_pick_tag">
                                                        <t t-esc="candidate.name"/>
                                                        <span class="as_tag_remove" t-on-mousedown.prevent.stop="() => this.removePendingButtonModel(candidate.id)">×</span>
                                                    </span>
                                                </t>
                                                <input type="text"
                                                       class="as_model_pick_input"
                                                       placeholder="Search models... e.g. Journal Entry, account.move"
                                                       t-att-value="this.state.wSearches.button_models"
                                                       t-on-input="(ev) => this.onButtonModelInput(ev)"
                                                       t-on-keydown="(ev) => this.onButtonModelKeydown(ev)"
                                                       t-on-focus="() => this.openDropdown('button_models')"
                                                       t-on-blur="() => this.scheduleClose('button_models')"/>
                                            </div>
                                            <button type="button"
                                                    t-att-class="'as_model_add_btn' + (this.state.wButtonModelCandidates.length ? '' : ' disabled')"
                                                    t-on-mousedown.prevent.stop="addSelectedButtonModels">Add Model Section</button>
                                        </div>
                                        <div class="as_model_catalog_wrap">
                                            <div t-if="this.state.wOpenField === 'button_models'" class="as_dropdown as_selector_panel" t-on-mousedown.stop="">
                                                <t t-foreach="this.getFiltered('button_models')" t-as="model" t-key="model.id">
                                                    <div class="as_dropdown_item" t-on-mousedown.prevent="() => this.selectOption('button_models', model)">
                                                        <i class="fa fa-cube as_dropdown_item_icon"/>
                                                        <span class="as_dropdown_item_text">
                                                            <span class="as_dropdown_item_title" t-esc="model.name"/>
                                                            <span class="as_dropdown_item_sub" t-esc="model.model"/>
                                                        </span>
                                                    </div>
                                                </t>
                                                <div t-if="!this.getFiltered('button_models').length" class="as_dropdown_empty">No results found.</div>
                                            </div>
                                        </div>
                                    </t>
                                    <t t-else="">
                                        <button type="button"
                                                class="as_model_add_btn"
                                                t-on-mousedown.prevent.stop="() => this.openDropdown('button_models')">+ Add Model Section</button>
                                    </t>
                                </div>
                            </div>
                        </t>

                        <!-- Filter/Group By -->
                        <t t-elif="this.state.wRestrictTab === 'filter'">
                            <p class="as_rtab_desc">Hide saved search filters and group-by choices from a model search view.</p>
                            <div class="as_filter_builder">
                                <t t-if="this.state.wFilterRules.length">
                                    <div class="as_filter_rules">
                                        <t t-foreach="this.state.wFilterRules" t-as="rule" t-key="rule.id">
                                            <div t-att-class="'as_filter_rule_card' + (this.isFilterModelCardOpen(rule.id) ? ' open' : '')">
                                                <div class="as_model_rule_head as_filter_rule_head" t-on-click="() => this.toggleFilterModelCard(rule)">
                                                    <div class="as_model_rule_icon as_filter_icon">
                                                        <i class="fa fa-search-minus"/>
                                                    </div>
                                                    <div>
                                                        <div class="as_model_rule_title" t-esc="rule.model_name"/>
                                                        <div class="as_model_rule_sub">
                                                            <t t-esc="rule.model"/> · <t t-esc="rule.nodes.length"/> items hidden
                                                        </div>
                                                    </div>
                                                    <span t-att-class="'as_model_rule_badge' + (rule.nodes.length ? '' : ' empty')">
                                                        <t t-esc="rule.nodes.length"/> items
                                                    </span>
                                                    <button type="button" class="as_model_remove_btn" t-on-click.stop="() => this.removeFilterRule(rule.id)" title="Remove">
                                                        <i class="fa fa-times"/>
                                                    </button>
                                                    <i class="fa fa-chevron-down as_model_rule_chevron"/>
                                                </div>
                                                <t t-if="this.isFilterModelCardOpen(rule.id)">
                                                    <div class="as_filter_rule_body">
                                                        <div class="as_form_group">
                                                            <label class="as_form_label">Filters / Group By</label>
                                                            <div class="as_tokenizer_wrap as_big_selector">
                                                                <div t-att-class="'as_tokenizer' + (this.state.wFilterModel ? '' : ' disabled')" t-on-click.stop="() => this.state.wFilterModel &amp;&amp; this.openDropdown('filter_nodes')">
                                                                    <input type="text" class="as_tokenizer_input"
                                                                           t-att-disabled="!this.state.wFilterModel"
                                                                           t-att-placeholder="this.state.wFilterModel ? 'Search filters/group by... e.g. Archived, Status, Salesperson' : 'Open the card first'"
                                                                           t-att-value="this.state.wSearches.filter_nodes"
                                                                           t-on-input="(ev) => this.onTokenizerInput(ev, 'filter_nodes')"
                                                                           t-on-focus="() => this.state.wFilterModel &amp;&amp; this.openDropdown('filter_nodes')"
                                                                           t-on-blur="() => this.scheduleClose('filter_nodes')"/>
                                                                </div>
                                                                <div t-if="this.state.wOpenField === 'filter_nodes'" class="as_dropdown as_selector_panel" t-on-mousedown.stop="">
                                                                    <div class="as_selector_panel_head">
                                                                        <div>
                                                                            <div class="as_selector_panel_title">Search View Items</div>
                                                                            <div class="as_selector_panel_sub">Select an item to hide · <t t-esc="this.getFiltered('filter_nodes').length"/> visible</div>
                                                                        </div>
                                                                        <button type="button" class="as_selector_close" t-on-mousedown.prevent.stop="() => this.closeDropdown()">
                                                                            <i class="fa fa-times"/>
                                                                        </button>
                                                                    </div>
                                                                    <t t-foreach="this.getFiltered('filter_nodes')" t-as="node" t-key="node.id">
                                                                        <div class="as_dropdown_item" t-on-mousedown.prevent="() => this.selectOption('filter_nodes', node)">
                                                                            <i t-att-class="node.node_type === 'group' ? 'fa fa-object-group as_dropdown_item_icon' : 'fa fa-filter as_dropdown_item_icon'"/>
                                                                            <span class="as_dropdown_item_text">
                                                                                <span class="as_dropdown_item_title" t-esc="node.name"/>
                                                                                <span class="as_dropdown_item_sub">
                                                                                    <t t-esc="node.node_type === 'group' ? 'Group By' : 'Filter'"/> · <t t-esc="node.attribute_name"/>
                                                                                </span>
                                                                            </span>
                                                                        </div>
                                                                    </t>
                                                                    <div t-if="!this.getFiltered('filter_nodes').length" class="as_dropdown_empty">No results found.</div>
                                                                </div>
                                                            </div>
                                                        </div>

                                                        <div t-if="rule.nodes.length" class="as_filter_node_grid">
                                                            <t t-foreach="rule.nodes" t-as="node" t-key="node.id">
                                                                <span t-att-class="'as_filter_node_chip ' + node.node_type">
                                                                    <i t-att-class="node.node_type === 'group' ? 'fa fa-object-group' : 'fa fa-filter'"/>
                                                                    <span>
                                                                        <t t-esc="node.name"/>
                                                                        <small t-esc="node.node_type === 'group' ? 'Group By' : 'Filter'"/>
                                                                    </span>
                                                                    <button type="button" t-on-click="() => this.removeFilterNode(rule.id, node.id)">×</button>
                                                                </span>
                                                            </t>
                                                        </div>
                                                    </div>
                                                </t>
                                            </div>
                                        </t>
                                    </div>
                                </t>
                                <t t-else="">
                                    <div class="as_model_placeholder">
                                        <i class="fa fa-search-minus"/> Click Add Model Section to create a filter/group-by section.
                                    </div>
                                </t>
                                <div class="as_filter_add_bar">
                                    <t t-if="this.state.wFilterPickerOpen || this.state.wFilterModelCandidates.length">
                                        <div class="as_model_pick_row">
                                            <div class="as_tokenizer as_model_pick_box" t-on-click.stop="() => this.openDropdown('filter_models')">
                                                <t t-foreach="this.state.wFilterModelCandidates" t-as="candidate" t-key="candidate.id">
                                                    <span class="as_tag as_tag_role as_model_pick_tag">
                                                        <t t-esc="candidate.name"/>
                                                        <span class="as_tag_remove" t-on-mousedown.prevent.stop="() => this.removePendingFilterModel(candidate.id)">×</span>
                                                    </span>
                                                </t>
                                                <input type="text"
                                                       class="as_model_pick_input"
                                                       placeholder="Search models... e.g. Invoice, account.move"
                                                       t-att-value="this.state.wSearches.filter_models"
                                                       t-on-input="(ev) => this.onFilterModelInput(ev)"
                                                       t-on-keydown="(ev) => this.onFilterModelKeydown(ev)"
                                                       t-on-focus="() => this.openDropdown('filter_models')"
                                                       t-on-blur="() => this.scheduleClose('filter_models')"/>
                                            </div>
                                            <button type="button"
                                                    t-att-class="'as_model_add_btn' + (this.state.wFilterModelCandidates.length ? '' : ' disabled')"
                                                    t-on-mousedown.prevent.stop="addSelectedFilterModels">Add Model Section</button>
                                        </div>
                                        <div class="as_model_catalog_wrap">
                                            <div t-if="this.state.wOpenField === 'filter_models'" class="as_dropdown as_selector_panel" t-on-mousedown.stop="">
                                                <t t-foreach="this.getFiltered('filter_models')" t-as="model" t-key="model.id">
                                                    <div class="as_dropdown_item" t-on-mousedown.prevent="() => this.selectOption('filter_models', model)">
                                                        <i class="fa fa-cube as_dropdown_item_icon"/>
                                                        <span class="as_dropdown_item_text">
                                                            <span class="as_dropdown_item_title" t-esc="model.name"/>
                                                            <span class="as_dropdown_item_sub" t-esc="model.model"/>
                                                        </span>
                                                    </div>
                                                </t>
                                                <div t-if="!this.getFiltered('filter_models').length" class="as_dropdown_empty">No results found.</div>
                                            </div>
                                        </div>
                                    </t>
                                    <t t-else="">
                                        <button type="button"
                                                class="as_model_add_btn"
                                                t-on-mousedown.prevent.stop="() => this.openDropdown('filter_models')">+ Add Model Section</button>
                                    </t>
                                </div>
                            </div>
                        </t>

                        <!-- Chatter -->
                        <t t-elif="this.state.wRestrictTab === 'chatter'">
                            <p class="as_rtab_desc">Control chatter and messaging visibility across all models.</p>
                            <div class="as_global_grid">
                                <div class="as_toggle_row">
                                    <div><div class="as_toggle_label">Hide Chatter</div><div class="as_toggle_sub">Compose Author</div></div>
                                    <label class="as_toggle_switch">
                                        <input type="checkbox" t-att-checked="this.state.wGlobalHideChatter ? 'checked' : undefined"
                                               t-on-change="() => this.toggleGlobal('wGlobalHideChatter')"/>
                                        <span class="as_toggle_track"/><span class="as_toggle_thumb"/>
                                    </label>
                                </div>
                                <div class="as_toggle_row">
                                    <div><div class="as_toggle_label">Hide Send Message</div><div class="as_toggle_sub">Compose Author</div></div>
                                    <label class="as_toggle_switch">
                                        <input type="checkbox" t-att-checked="this.state.wGlobalHideSendMessage ? 'checked' : undefined"
                                               t-on-change="() => this.toggleGlobal('wGlobalHideSendMessage')"/>
                                        <span class="as_toggle_track"/><span class="as_toggle_thumb"/>
                                    </label>
                                </div>
                                <div class="as_toggle_row">
                                    <div><div class="as_toggle_label">Hide Log Notes</div><div class="as_toggle_sub">Internal note button</div></div>
                                    <label class="as_toggle_switch">
                                        <input type="checkbox" t-att-checked="this.state.wGlobalHideLogNote ? 'checked' : undefined"
                                               t-on-change="() => this.toggleGlobal('wGlobalHideLogNote')"/>
                                        <span class="as_toggle_track"/><span class="as_toggle_thumb"/>
                                    </label>
                                </div>
                                <div class="as_toggle_row">
                                    <div><div class="as_toggle_label">Hide Schedule Activity</div><div class="as_toggle_sub">Activity scheduling</div></div>
                                    <label class="as_toggle_switch">
                                        <input type="checkbox" t-att-checked="this.state.wGlobalHideActivity ? 'checked' : undefined"
                                               t-on-change="() => this.toggleGlobal('wGlobalHideActivity')"/>
                                        <span class="as_toggle_track"/><span class="as_toggle_thumb"/>
                                    </label>
                                </div>
                            </div>
                        </t>

                        <!-- Global -->
                        <t t-elif="this.state.wRestrictTab === 'global'">
                            <p class="as_rtab_desc">System-wide settings applied globally across all models.</p>
                            <div class="as_global_section_label">IMPORT &amp; EXPORT</div>
                            <div class="as_global_grid">
                                <div class="as_toggle_row">
                                    <div><div class="as_toggle_label">Hide Import</div><div class="as_toggle_sub">CSV/Excel import</div></div>
                                    <label class="as_toggle_switch">
                                        <input type="checkbox" t-att-checked="this.state.wGlobalHideImport ? 'checked' : undefined"
                                               t-on-change="() => this.toggleGlobal('wGlobalHideImport')"/>
                                        <span class="as_toggle_track"/><span class="as_toggle_thumb"/>
                                    </label>
                                </div>
                                <div class="as_toggle_row">
                                    <div><div class="as_toggle_label">Hide Export</div><div class="as_toggle_sub">Export to file</div></div>
                                    <label class="as_toggle_switch">
                                        <input type="checkbox" t-att-checked="this.state.wGlobalHideExport ? 'checked' : undefined"
                                               t-on-change="() => this.toggleGlobal('wGlobalHideExport')"/>
                                        <span class="as_toggle_track"/><span class="as_toggle_thumb"/>
                                    </label>
                                </div>
                                <div class="as_toggle_row">
                                    <div><div class="as_toggle_label">Hide Spreadsheet</div><div class="as_toggle_sub">Insert in spreadsheet</div></div>
                                    <label class="as_toggle_switch">
                                        <input type="checkbox" t-att-checked="this.state.wGlobalHideSpreadsheet ? 'checked' : undefined"
                                               t-on-change="() => this.toggleGlobal('wGlobalHideSpreadsheet')"/>
                                        <span class="as_toggle_track"/><span class="as_toggle_thumb"/>
                                    </label>
                                </div>
                                <div class="as_toggle_row">
                                    <div><div class="as_toggle_label">Hide Add Property</div><div class="as_toggle_sub">Custom properties</div></div>
                                    <label class="as_toggle_switch">
                                        <input type="checkbox" t-att-checked="this.state.wGlobalHideAddProperty ? 'checked' : undefined"
                                               t-on-change="() => this.toggleGlobal('wGlobalHideAddProperty')"/>
                                        <span class="as_toggle_track"/><span class="as_toggle_thumb"/>
                                    </label>
                                </div>
                            </div>
                            <div class="as_global_section_label">ACCESS CONTROL</div>
                            <div class="as_global_grid">
                                <div class="as_toggle_row">
                                    <div><div class="as_toggle_label">Force Read-Only</div><div class="as_toggle_sub">Entire system readonly</div></div>
                                    <label class="as_toggle_switch">
                                        <input type="checkbox" t-att-checked="this.state.wGlobalForceReadonly ? 'checked' : undefined"
                                               t-on-change="() => this.toggleGlobal('wGlobalForceReadonly')"/>
                                        <span class="as_toggle_track"/><span class="as_toggle_thumb"/>
                                    </label>
                                </div>
                                <div class="as_toggle_row">
                                    <div><div class="as_toggle_label">Disable Developer Mode</div><div class="as_toggle_sub">Block ?debug=1</div></div>
                                    <label class="as_toggle_switch">
                                        <input type="checkbox" t-att-checked="this.state.wGlobalDisableDevMode ? 'checked' : undefined"
                                               t-on-change="() => this.toggleGlobal('wGlobalDisableDevMode')"/>
                                        <span class="as_toggle_track"/><span class="as_toggle_thumb"/>
                                    </label>
                                </div>
                                <div class="as_toggle_row">
                                    <div><div class="as_toggle_label">Hide Technical Settings</div><div class="as_toggle_sub">Settings/Technical menu</div></div>
                                    <label class="as_toggle_switch">
                                        <input type="checkbox" t-att-checked="this.state.wGlobalHideTechSettings ? 'checked' : undefined"
                                               t-on-change="() => this.toggleGlobal('wGlobalHideTechSettings')"/>
                                        <span class="as_toggle_track"/><span class="as_toggle_thumb"/>
                                    </label>
                                </div>
                            </div>
                        </t>
                    </t>

                    <!-- Step 4: Review -->
                    <t t-elif="this.state.wizardStep === 4">
                        <div class="as_review_summary">
                            <h3 class="as_review_title">Summary</h3>
                            <t t-set="reviewRows" t-value="this.getReviewSummaryRows()"/>
                            <t t-if="reviewRows.length">
                                <t t-foreach="reviewRows" t-as="row" t-key="row.key">
                                    <div class="as_review_item">
                                        <i t-att-class="row.icon + ' as_review_icon'"/>
                                        <span><strong><t t-esc="row.label"/>:</strong> <t t-esc="row.detail"/></span>
                                    </div>
                                </t>
                            </t>
                            <t t-else="">
                                <div class="as_review_item">
                                    <i class="fa fa-info-circle as_review_icon"/>
                                    <span><em>No restrictions configured.</em></span>
                                </div>
                            </t>
                        </div>

                        <div class="as_review_warning">
                            <i class="fa fa-exclamation-triangle"/>
                            <p><t t-esc="this.getReviewWarningText()"/></p>
                        </div>

                        <button class="as_btn as_btn_outline as_btn_full" t-on-click="saveAsDraft">
                            Save as Draft
                        </button>
                    </t>

                </div><!-- /modal body -->

                <!-- Modal footer -->
                <div class="as_modal_footer">
                    <t t-if="this.state.wizardStep > 1">
                        <button class="as_btn as_btn_outline" t-on-click="wizardBack">Back</button>
                    </t>
                    <button class="as_btn as_btn_outline" t-on-click="closeWizard">Cancel</button>
                    <button class="as_btn as_btn_primary" t-on-click="wizardNext">
                        <t t-if="this.state.wizardStep === 4">
                            <t t-if="this.state.wizardMode === 'edit'">Save Changes</t>
                            <t t-else="">Publish Rule</t>
                        </t>
                        <t t-else="">Continue</t>
                    </button>
                </div>

            </div>
        </div>
    </t>

    <!-- ── Delete Confirm Dialog ───────────────────────────────────────────── -->
    <t t-if="this.state.confirmOpen and this.state.confirmRule">
        <div class="as_confirm_overlay" t-on-click.self="cancelDelete">
            <div class="as_confirm_dialog">
                <div class="as_confirm_icon_wrap">
                    <i class="fa fa-trash-o as_confirm_icon"/>
                </div>
                <div class="as_confirm_title">Delete Rule?</div>
                <div class="as_confirm_rule_name">
                    "<t t-esc="this.state.confirmRule.name"/>"
                </div>
                <p class="as_confirm_msg">
                    This will permanently remove the rule and all its restrictions.
                    Any hidden menus or access blocks applied by this rule will be lifted after reload.
                </p>
                <div class="as_confirm_actions">
                    <button class="as_btn as_btn_outline as_confirm_cancel" t-on-click="cancelDelete">
                        Cancel
                    </button>
                    <button class="as_btn as_btn_danger" t-on-click="confirmDelete">
                        <i class="fa fa-trash-o"/> Delete Rule
                    </button>
                </div>
            </div>
        </div>
    </t>

</div>
</t>
</templates>
